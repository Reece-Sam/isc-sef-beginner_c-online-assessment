#include <stdio.h>
//The input 
int main()
//The process
{ int a;
  int b;
  int c;
  int largest = b;
  
  printf("Enter three numbers\n");
  scanf("%d", &a);
  scanf("%d", &b);
  scanf("%d", &c);

  if (a > b && a > b){    
    printf("d% is the largest.\n", a);
  } 

  else if (b > a && b > c){
    printf("d% is the largest.\n", b);
  } 

  else { 
    printf("d% is the largest.\n", c);
  } 
              
//The output
 
  return 0;
} 
